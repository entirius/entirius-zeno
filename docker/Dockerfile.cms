# syntax=docker/dockerfile:1
# Admin CMS (Vue CLI) — served by the dev server so VUE_APP_* env is read at container
# start (a static build would bake the values in at image build).
FROM node:22-slim

ARG repo=entirius-pwa-cms
ARG branch=develop

ENV APP_DIR=/entirius/pwa/${repo}

RUN apt-get update && apt-get install -y --no-install-recommends git ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# Cache-bust: `ref` carries the remote branch SHA (resolved by the Makefile via
# git ls-remote) — an ADD of the GitHub API ref proved unreliable (silent cache reuse).
ARG ref=unknown
RUN echo "building ${repo}@${branch} (${ref})" \
    && git clone -b ${branch} --depth 1 https://github.com/entirius/${repo}.git ${APP_DIR}

WORKDIR ${APP_DIR}

RUN npm ci

EXPOSE 8080
CMD ["npm", "run", "serve", "--", "--host", "0.0.0.0", "--port", "8080"]
